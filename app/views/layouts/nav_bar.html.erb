<nav class="navbar navbar-inverse">
<div class="container-fluid">
    <div class="navbar-header">
    <%= link_to "PetBook", posts_path, class: "navbar-brand"%>
    </div>
    <div class="collapse navbar-collapse">
    <form class="navbar-form navbar-right" action="/search">
        <input class="form-control" name="q" type="text" placeholder="Search">
        <button class="btn btn-sm" type="submit">
        <span class="glyphicon glyphicon-search"></span>
        </button>
    </form>
        <% if user_signed_in? %>
            <ul class="nav navbar-nav navbar-right">
                <li><%= link_to 'Profile', edit_user_registration_path, :class => "btn-default"%></li>
                <li><%= link_to 'Log-out', destroy_user_session_path, method: :delete, :class => "glyphicon glyphicon-log-out" %></li>
        <% else %>
                <li><%= link_to 'Log In', new_user_session_path, :class => "btn-default" %></li>
                <li><%= link_to 'Sign Up', new_user_registration_path, :class => "btn-default" %></li>
            </ul>
        <% end %>
    </div>
</div>
</nav>