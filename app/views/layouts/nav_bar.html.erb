<nav class="navbar navbar-inverse">
<div class="container-fluid">
    <div class="navbar-header">
    <%= link_to "PetBook", posts_path, class: "navbar-brand mb-0"%>
    </div>
    <div class="collapse navbar-collapse">
    <form class="navbar-form navbar-right" action="/search">
        <input class="form-control" name="q" type="text" placeholder="Search">
        <button class="btn btn-sm" type="submit">
        <span class="glyphicon glyphicon-search"></span>
        </button>
    </form>
        <% if user_signed_in? %>
            <div class="nav navbar-nav navbar-left">
                <p class="navbar-brand mb-2">Welcome, <%= current_user.name%></p>      
            </div>
            <ul class="nav navbar-nav navbar-right">
                <li><%= link_to ' Profile', edit_user_registration_path, :class => "glyphicon glyphicon-user"%></li>
                <li><%= link_to ' Log-out', destroy_user_session_path, method: :delete, :class => "glyphicon glyphicon-log-out" %></li>
        <% else %>
                <li><%= link_to ' Log In', new_user_session_path, :class => "glyphicon glyphicon-log-in" %></li>
                <li><%= link_to ' Sign Up', new_user_registration_path, :class => "glyphicon glyphicon-user" %></li>
            </ul>
        <% end %>
    </div>
</div>
</nav>