<div class="row">
    <% @users.each do |user| %>
    <div class="col-sm-4">
        <div class="sm-profile-container">
            <% if user.image %>
              <%= image_tag user.image, class: "profile-img", alt:"Avatar"%>
            <% end %>
            <p><%= link_to user.name, user %></p>
            <p><%= user.bio %></p>
            <p>Number of pets: <%= user.pet_count %></p>
            <% user.species_count.each do |k, v| %>  
                    <p><%= k %>: <%= v %></p>
            <% end %>
        </div>
    </div>
    <% end %>
</div>




